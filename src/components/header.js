import React, { Component } from 'react';

class Header extends Component {
    render() {
        return (
            <header>
                Daily News
            </header>
        );
    }
}

export default Header;